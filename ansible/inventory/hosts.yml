---
# Maintenance inventory: KNOWN VMs, for direct runs of the phase
# playbooks (04/05/06/02 re-runs, 03_storage_verify, ad-hoc commands).
# The DEPLOY flow does not use this file - it uses provisioning.yml
# (localhost + empty `provisioned` group; 00_provision_vm.yml add_host's
# each fresh VM at runtime) - so a deploy never accidentally touches
# the VMs listed here.
all:
  children:
    provisioned:
      hosts:
        vm-test-01:
          ansible_host: 192.168.122.50
          ansible_user: ubuntu
