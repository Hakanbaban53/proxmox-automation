---
# Phase 1 inventory: the freshly provisioned test VM.
# Keep the IP in sync with terraform.tfvars (vm_ip_address).
# Phase 4 replaces this static file with dynamic inventory fed by Terraform.
all:
  children:
    provisioned:
      hosts:
        vm-test-01:
          ansible_host: 192.168.122.50
          ansible_user: ubuntu
